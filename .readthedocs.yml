version: 2
build:
  os: "ubuntu-24.04"
  tools:
    python: "miniconda-latest"
sphinx:
  configuration: doc/conf.py
formats: all
conda:
  environment: yeadon-dev-env.yml
python:
  install:
    - method: setuptools
      path: .
