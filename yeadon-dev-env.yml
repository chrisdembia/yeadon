name: yeadon-dev
channels:
  - conda-forge
# NOTE : The minimum versions correspond to those in Ubuntu 24.04 LTS.
dependencies:
  - coverage >=7.4.4
  - nose >=1.3.7
  - numpy >=1.26.4
  - numpydoc >=1.6.0
  - pyyaml >=6.0.1
  - setuptools >=68.1.2
  - sphinx >=7.2.6
